name: Phase 8 Shared AST Closeout Runner

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  closeout:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/kdsl-phase8-p1l-shared-ast-closeout
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Execute bounded closeout patch
        run: |
          python - <<'PY'
          from pathlib import Path
          source = Path('.github/workflows/phase8-closeout-patch.yml')
          text = source.read_text(encoding='utf-8')
          start_marker = "          python - <<'PY'\n"
          end_marker = "          PY\n"
          start = text.index(start_marker) + len(start_marker)
          end = text.index(end_marker, start)
          lines = text[start:end].splitlines()
          script = '\n'.join(line[10:] if line.startswith('          ') else line for line in lines) + '\n'
          exec(compile(script, str(source), 'exec'), {'__name__': '__main__'})
          runner = Path('.github/workflows/phase8-closeout-run.yml')
          if runner.exists():
              runner.unlink()
          PY

      - name: Run bounded closeout verification
        run: |
          python tools/validator/run_p1_shared_ast_samples.py
          python tools/validator/run_p1_contract_samples.py
          python tools/validator/run_packet_p1_normalization_samples.py

      - name: Commit closeout alignment
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Docs: align Phase 8 shared AST closeout"
          git push origin HEAD:agent/kdsl-phase8-p1l-shared-ast-closeout
