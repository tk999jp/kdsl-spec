name: Phase 9D Adoption Sync

on:
  pull_request:
    branches: [main]
    types: [synchronize]

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]' && github.head_ref == 'phase9d-binding-schema'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: phase9d-binding-schema
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Align adoption state
        run: python tools/validator/tmp_phase9d_adopt.py
      - name: Validate repository
        run: python tools/validator/run_all_samples.py
      - name: Store adopted schema state
        run: |
          rm .github/workflows/phase9d-adopt-sync.yml
          rm tools/validator/tmp_phase9d_adopt.py
          rm .phase9d-adopt-trigger
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Spec: finalize Phase 9D schema adoption state"
          git push origin HEAD:phase9d-binding-schema
