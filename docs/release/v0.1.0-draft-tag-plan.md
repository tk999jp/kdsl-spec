# v0.1.0-draft Tag Plan

status: tag-plan
phase: Phase 8 v0.1.0-draft tag decision
decision: pending_explicit_user_tag_approval
tag_created: no
release_created: no
publicized: no

## 1. Purpose

v0.1.0-draft tag作成の可否を判断するための計画文書。

この文書はtag作成そのものではない。

## 2. Current readiness

checklist:

```text
docs/reviews/v0.1.0-draft-checklist.md
```

現時点判断:

```text
v0.1.0-draft tag: candidate_after_user_review
release: not recommended
public: not recommended
```

## 3. Required user approval

```text
U承認なしにtag作成禁止
"続けてください" は作業継続承認として扱う
tag作成そのものは "v0.1.0-draft tagを作成してよい" 等の明示承認が必要
```

## 4. Recommended tag type

推奨:

```text
annotated tag
name: v0.1.0-draft
```

理由:

```text
仕様正本候補の節目として、tag messageを残せる
release/public化なしでも、private repo内の参照点として使える
```

非推奨:

```text
lightweight tag
```

理由:

```text
draft仕様の判断記録がtag側に残らない
```

## 5. Suggested tag message

```text
v0.1.0-draft

Initial private draft of KDSL/R1 specification.
Includes Core, Profiles, R1, Lint, Bridge, Templates, Examples, Validator design, Manifest, Glossary, and draft review checklist.
No validator implementation.
No public release.
```

## 6. Forbidden without additional approval

```text
release作成禁止
public化禁止
Release Assets作成禁止
tag移動/上書き前提禁止
force update禁止
```

## 7. If approved

実行候補:

```text
create annotated tag v0.1.0-draft at current main HEAD
no GitHub Release
no publicization
```

実行後の記録候補:

```text
README.md status/tag更新
CHANGELOG.md tag記録
docs/release/v0.1.0-draft-tag-plan.md result更新
```

## 8. If not approved

```text
tag未作成のまま継続
次Phaseは overview / public-readiness / validator MVP design のいずれか
```

## 9. R1-style decision result

```text
KDSL_RESULT:
STATUS: partial
PHASE: v0.1.0-draft tag decision
S: tag planを追加。v0.1.0-draft tagは候補だが、tag作成は明示U承認待ち。
FILES:
  changed:
    - docs/release/v0.1.0-draft-tag-plan.md
WHY: tag作成を安全に分離し、続行承認とtag作成承認を混同しないため。
CMD:
  - create_file docs/release/v0.1.0-draft-tag-plan.md
VERIFY:
  executed:
    - tag plan written
  not_run:
    - tag creation
    - release creation
RT: na
RISK:
  - tag未作成
  - release/public化は非推奨
NEXT: Uが明示承認する場合のみannotated tag v0.1.0-draftを作成。実行許可ではない。
COMMIT:
  actual: GitHub API commit for this file
  proposed: Docs: add v0.1.0 draft tag plan
```
