# KDSL v1.1.0-rc1 Release Notes

status: rc1
release: v1.1.0-rc1
public: yes
GitHub Release: created
Release Assets: none

## Important notice

This is a release candidate, not a stable production release.

No binary assets are attached.

This release does not imply validator-based approval delegation, runtime verification, full semantic equivalence, or production stability.

## Summary

KDSL v1.1 aligns the core prompt notation with ADPS-aware boundary rules and expands the R1 validator MVP into a multi-slice validation toolkit.

## Highlights

```text
Core KDSL v1.1-ADPS-aware sync
KDSL / KDSL-DP / ADPS bridge update
R1 required-block checker
RT basis checker
NEXT/COMMIT authority guard
KDSL template reference checker
KDSL template expansion checker
combined validator wrapper with r1 / prompt / all targets
public-facing README and examples draft set
```

## Validator slices

```text
r1_required_blocks.py:
  checks required KDSL_RESULT fields

r1_rt_basis.py:
  checks RT:v basis wording

r1_authority_guard.py:
  checks NEXT proposal-only and COMMIT actual/proposed/none shape

kdsl_template_refs.py:
  checks known template references and template safety gates

kdsl_template_expansion.py:
  checks expansion/inheritance evidence and required section groups

kdsl_validate.py:
  wraps checks by target: r1 / prompt / all
```

## Safety boundaries

```text
validator pass != U approval
validator pass != RT:v
validator pass != implementation validity
validator pass != semantic equivalence
validator pass != release readiness
NEXT := proposal-only
COMMIT := actual/proposed/none record field
KDSL-DP直接実行禁止
P1/P1L正規化必須
```

## Known limitations

```text
No full natural-language semantic parser
No runtime execution by validator
No source authenticity judgment
No approval delegation
No production stability claim
No binary Release Assets
```

## Release status

```text
release: v1.1.0-rc1
release_type: prerelease
public: yes
repository_visibility: public
tag: v1.1.0-rc1
Release Assets: none
```
